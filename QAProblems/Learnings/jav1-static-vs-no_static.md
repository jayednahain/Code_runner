# Static vs. Non-Static
- static methods belong to the class itself and can be called without creating an instance of the class.
- Non-static (instance) methods belong to objects of the class and can only be called on an object instance.
